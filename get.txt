// post data

Post.
	.id
	.title
	.content
	.tag_list
	.answers_count
	.impressions_count
	.comments_count [ADD]
	.vote_count
	.created_at_ago

	.user
		.id
		.first_name_with_last_initial
		.profile_image_url
		.position 
		.hq_state 
		.org_name 
		.credits_count [ADD]
		.milestones_count 

	.comments
		.created_at_ago
		.id
		.content
		.vote_count
		.user
			.id
			.first_name_with_last_initial
			.profile_image_url
			.position 
			.hq_state 
			.org_name 
			.credits_count [ADD]
			.milestones_count

	.answers
		.created_at_ago 
		.user
		.id
		.content
		.vote_count
		.comments_count [ADD]
		.first_name_with_last_initial
		.profile_image_url
		.position 
		.hq_state 
		.org_name [ADD]
		.credits_count [ADD]
		.milestones_count [ADD]
		.comments
			.created_at_ago [ADD]
			.id
			.content
			.vote_count
				.user
					.id
					.first_name_with_last_initial
					.profile_image_url
					.position 
					.hq_state 
					.org_name 
					.credits_count [ADD]
					.milestones_count 

// User Data

Main User (the person who logs in to the app) 
	.id
	.first_name_with_last_initial
	.profile_image_url
	.position 
	.hq_state 
	.org_name 
	.credits_count [ADD]
	.milestones_count 
	.tag_list (SAME NAMING?) [ADD]
	.personal_profile [ADD]
		.about
		.goals
	[BUSINESS ONLY]
	.funding [ADD]
		.sought
		.purpose
		.priority
	.recommendaiton_profile [ADD]
	 	.business_type
	 	.customers_list
	 	.business_age
	 	.employees_count
	 	.ar
	 	.revenue_size
	 	.industry
	 	.does_business_in

	.post [post generated by users]

	.follwing
		.id
		[the whole user profile]

	.follwers
		.id
		[the whole user profile]

	.messages
		.















